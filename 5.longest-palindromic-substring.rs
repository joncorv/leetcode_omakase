// @leet start
impl Solution {
    pub fn longest_palindrome(s: String) -> String {
        // define values
        let s_len = s.len();
        println!("The length of s is: {s_len}");
        let result: String = "".to_string();
        println!("the result string has been instantiated to: {result}");

        let s: Vec<char> = s.chars().collect();
        println!("s has been shadow copied to: {:?}", &s);

        return "testing".to_string();
    }
}
// @leet end
